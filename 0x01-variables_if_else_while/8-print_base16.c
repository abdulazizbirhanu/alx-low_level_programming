#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'the progra's descriptions'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n = 0;

while (n <= 15)
{
	printf("%x", n);
	n++;
}
	putchar('\n');
	return (0);
}
